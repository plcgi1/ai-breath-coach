# Улучшения фронтенда для работы с тарифами

## Обзор изменений

В рамках улучшения фронтенда в `/packages/frontend-v2` были реализованы ключевые функции для работы с тарифами и лимитами, соответствующие бизнес-логике Telegram Mini App для дыхательных практик.

## Реализованные функции

### 1. Система управления тарифами
- **FREE**: Доступны 3 базовые техники, лимит — 1 сессия в день на всё
- **N-ITEMS**: Покупка конкретных PRO-техник на срок (30 дней), снимает дневной лимит для купленной техники
- **LIFETIME**: Разовая покупка всего контента, снимает все лимиты, открывает кастомизацию слотов

### 2. Обновленные API методы
- `getUserLayoutAndLimits()` - получение конфигурации 3-х кнопок и флага лимита
- `updateSlot(slotIndex, techniqueId)` - изменение техники в конкретном слоте

### 3. Новая система лимитов (`src/lib/stores/limits.js`)
- Отслеживание суточных сессий
- Проверка лимитов перед началом практики
- Интеграция с системой подписки

### 4. Улучшенный компонент ModesPanel (`src/components/ModesPanel.svelte`)
- Возможность просмотра всех режимов
- Фильтрация по типу (бесплатные/PRO)
- Интерфейс для изменения слотов (доступен только премиум-пользователям)
- Улучшенный UX с модальным окном выбора слота

### 5. Обновленный MoodSelector (`src/components/MoodSelector.svelte`)
- Использует layout, полученный с сервера
- Проверяет лимиты перед началом практики
- Интеграция с системой лимитов

### 6. Обновленный breathing controller (`src/lib/stores/breathing.js`)
- Интеграция с системой лимитов
- Проверка лимита перед началом сессии
- Автоматическое завершение сессии с обновлением статистики

### 7. Обновленный PurchasePanel (`src/components/PurchasePanel.svelte`)
- Сброс лимитов после успешной покупки
- Обновление статуса пользователя

## Архитектурные изменения

### Структура данных
```javascript
// Пример ответа от API /user/layout-and-limits
{
  layout: [
    { id: 'relaxing', name: '4-7-8', icon: '😌', color: 'from-blue-400 to-purple-500', slug: 'calm' },
    { id: 'box', name: 'Квадрат', icon: '📦', color: 'from-purple-400 to-pink-500', slug: 'focus' },
    { id: 'energizing', name: 'Энергия', icon: '⚡', color: 'from-orange-400 to-red-500', slug: 'energy' }
  ],
  limitReached: false,
  dailySessions: 0,
  maxDailySessions: 1
}
```

### Система лимитов
- FREE пользователи: 1 сессия в день на всё
- PRO пользователи: без ограничений на количество сессий
- Система отслеживает дату последней сессии и количество сессий за день

## Технические детали

### Интеграция с Telegram Mini Apps
- Поддержка haptic feedback
- Интеграция с системой оплаты через Telegram Stars
- Использование Telegram Web Apps API

### Поддержка offline режима
- Fallback на локальные данные при отсутствии сети
- Сохранение прогресса в localStorage

## Используемые технологии
- **Svelte 4** - реактивный фреймворк
- **Tailwind CSS** - стилизация
- **Telegram Web Apps** - интеграция с Telegram
- **PostgreSQL** - backend (через API)

## Дальнейшие шаги
1. Интеграция с backend для полноценной работы системы лимитов
2. Добавление аналитики использования тарифов
3. Реализация системы "подарочных" подписок
4. Добавление функции "семейного" доступа