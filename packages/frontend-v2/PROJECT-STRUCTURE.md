# 🧘 BreathFlow — Полная структура проекта

## 📁 Файловая структура

```
breathflow/
│
├── 📱 ФРОНТЕНД (Telegram WebApp)
│   ├── index.html              # Основное приложение
│   ├── landing.html            # Лендинг для сбора трафика
│   └── content-plan.html       # Контент-план для продвижения
│
├── 🖥️ СЕРВЕР (выберите один)
│   │
│   ├── server/                 # Node.js + Express (для разработки)
│   │   ├── package.json
│   │   └── index.js
│   │
│   └── cloudflare-worker/      # Cloudflare Workers (бесплатный продакшен)
│       ├── wrangler.toml
│       ├── package.json
│       ├── README.md
│       └── src/
│           └── index.js
│
├── 🤖 TELEGRAM BOT
│   └── telegram-bot/
│       ├── package.json
│       └── bot.js              # Напоминания, онбординг
│
├── 🎨 SVELTE ВЕРСИЯ (опционально)
│   └── svelte-app/
│       ├── package.json
│       ├── vite.config.js
│       ├── tailwind.config.js
│       └── src/
│           ├── App.svelte
│           ├── main.js
│           └── components/
│
└── 📚 ДОКУМЕНТАЦИЯ
    ├── PROJECT-STRUCTURE.md    # Этот файл
    └── README.md
```

---

## 🚀 Быстрый старт

### Вариант 1: Минимальный (только фронтенд)

```bash
# 1. Откройте index.html в браузере или через Live Server

# 2. Создайте бота
# Откройте @BotFather → /newbot → получите токен

# 3. Залейте на хостинг
# Cloudflare Pages, Vercel, GitHub Pages — бесплатно

# 4. Добавьте WebApp в бота
# @BotFather → /mybots → ваш бот → Bot Settings → Menu Button
```

**Стоимость: $0**
**Возможности:** Базовое приложение, без оплаты

---

### Вариант 2: С оплатой (Cloudflare Workers)

```bash
# 1. Установите Wrangler
npm install -g wrangler
wrangler login

# 2. Создайте KV Namespaces
wrangler kv:namespace create "USERS"
wrangler kv:namespace create "PAYMENTS"
wrangler kv:namespace create "ANALYTICS"

# 3. Добавьте секреты
wrangler secret put BOT_TOKEN
wrangler secret put WEBHOOK_SECRET

# 4. Обновите wrangler.toml с вашими ID

# 5. Деплой
cd cloudflare-worker
wrangler deploy

# 6. Настройте webhook
# Откройте: https://your-worker.workers.dev/api/setup-webhook?url=https://your-worker.workers.dev/webhook/telegram

# 7. Обновите API_URL в index.html
```

**Стоимость: $0** (до 100K запросов/день)
**Возможности:** Полная монетизация через Telegram Stars

---

### Вариант 3: Полный стек

```bash
# 1. Сервер API (Cloudflare Workers)
cd cloudflare-worker && wrangler deploy

# 2. Фронтенд (Cloudflare Pages)
# Dashboard → Pages → Create → Upload index.html

# 3. Бот напоминаний
cd telegram-bot
npm install
npm start
# Или интегрируйте в Worker

# 4. Настройка webhook для бота
```

---

## 💰 Модель монетизации

### Продукты

| ID | Название | Цена | Тип |
|----|----------|------|-----|
| `technique_anti-panic` | Антипаника 911 | 99 ⭐ | Техника |
| `technique_wim-hof` | Метод Вим Хофа | 199 ⭐ | Техника |
| `technique_energizer` | Кофеиновое дыхание | 99 ⭐ | Техника |
| `technique_sleep-pro` | Глубокий сон PRO | 149 ⭐ | Техника |
| `course_sleep` | Курс "Здоровый сон" | 149 ⭐ | Курс |
| `course_anxiety` | Курс "Без тревоги" | 249 ⭐ | Курс |
| `lifetime` | Всё навсегда | 990 ⭐ | Lifetime |

### Юнит-экономика

| Метрика | Значение |
|---------|----------|
| Конверсия в покупку | 3-5% |
| Средний чек | 150 ⭐ (~$3) |
| LTV | $10-15 |
| CAC (органика) | $0-0.50 |

### Прогноз дохода

| Пользователей | Покупателей | Доход/мес |
|---------------|-------------|-----------|
| 1,000 | 30-50 | $90-150 |
| 10,000 | 300-500 | $900-1,500 |
| 50,000 | 1,500-2,500 | $4,500-7,500 |

---

## 📡 API Endpoints

### Техники

```
GET  /api/breathing/:slug        # Получить технику
POST /api/breathing/ai           # AI рекомендация
GET  /api/products               # Список продуктов
GET  /api/techniques             # Список техник
```

### Платежи

```
POST /api/payments/create-invoice    # Создать invoice
GET  /api/payments/status/:userId    # Статус покупок
GET  /api/payments/history/:userId   # История
```

### Аналитика

```
POST /api/analytics/event        # Отправить событие
GET  /api/analytics/dashboard    # Дашборд
```

### Напоминания

```
POST /api/reminders/set          # Установить напоминание
GET  /api/cron/send-reminders    # Отправить (cron)
```

### Telegram

```
POST /webhook/telegram           # Webhook
GET  /api/setup-webhook?url=     # Настройка
```

---

## 🌍 Регионы и платежи

### Россия + СНГ

```
Пользователь → Telegram Stars → Fragment → TON → P2P биржа → Карта
```

**Комиссии:** ~3-5%

### Европа + США

```
Пользователь → Telegram Stars → Fragment → TON → Binance/Kraken → Банк
```

**Или:** То же самое, Stars работают глобально

---

## 📊 Аналитика

### Отслеживаемые события

| Событие | Описание |
|---------|----------|
| `bot_start` | Запуск бота |
| `webapp_open` | Открытие приложения |
| `technique_view` | Просмотр техники |
| `technique_start` | Начало практики |
| `technique_complete` | Завершение практики |
| `ai_query` | Запрос к AI |
| `invoice_created` | Создан invoice |
| `purchase_completed` | Успешная покупка |

### Дашборд

```bash
curl https://your-worker.workers.dev/api/analytics/dashboard
```

```json
{
  "metrics": {
    "today": {
      "bot_start": 45,
      "technique_view": 120,
      "purchase_completed": 3
    }
  },
  "revenue": {
    "totalStars": 8500,
    "totalUSD": "170.00",
    "totalPurchases": 45
  },
  "conversionRate": "2.5%"
}
```

---

## 🤖 Бот — команды

| Команда | Описание |
|---------|----------|
| `/start` | Приветствие + меню |
| `/breathe` | Быстрая практика в чате |
| `/remind` | Настройка напоминаний |
| `/stats` | Статистика пользователя |
| `/help` | Помощь |

---

## 📅 Roadmap

### Фаза 1: MVP (Неделя 1-2)
- [x] Базовое приложение
- [x] 3 бесплатные техники
- [x] Telegram Stars интеграция
- [ ] Деплой на Cloudflare

### Фаза 2: Монетизация (Неделя 3-4)
- [ ] 5 PRO техник
- [ ] Webhook для платежей
- [ ] Аналитика покупок
- [ ] Первые продажи

### Фаза 3: Рост (Месяц 2-3)
- [ ] Бот с напоминаниями
- [ ] Реферальная программа
- [ ] Курсы с аудио
- [ ] 1000+ пользователей

### Фаза 4: Масштаб (Месяц 4-6)
- [ ] AI рекомендации (GPT)
- [ ] Интеграция с Apple Watch
- [ ] Локализация (EN)
- [ ] 10,000+ пользователей

---

## 🔧 Технологии

| Компонент | Технология | Стоимость |
|-----------|------------|-----------|
| Фронтенд | HTML + Tailwind + Vanilla JS | $0 |
| Хостинг фронта | Cloudflare Pages | $0 |
| API | Cloudflare Workers | $0 |
| База данных | Cloudflare KV | $0 |
| Платежи | Telegram Stars | 0%* |
| Аналитика | Свой код + KV | $0 |

*Telegram берёт ~30% при покупке Stars пользователем

---

## 📞 Контакты

- Telegram: @breathflow_bot
- Support: @breathflow_support
- GitHub: github.com/your-repo

---

**Создано для инди-разработчиков с минимальным бюджетом 🚀**
