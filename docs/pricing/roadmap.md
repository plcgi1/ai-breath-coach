Для создания устойчивой бизнес-модели мобильного приложения важно сбалансировать пользу для пользователя и мотивацию к покупке. Психология в дыхательных практиках строится на **формировании привычки** и **снятии острой боли** (стресс, бессонница).

Ниже представлены 4 тарифа, их психологическое обоснование и архитектура для реализации на стороне сервера (NestJS).

---

## 1. Психологическая сетка тарифов

| Тариф | Психологическое обоснование | Целевая аудитория |
| --- | --- | --- |
| **Free (Базовый)** | **"Эффект владения"**. Даем попробовать продукт без риска. Лимит "1 раз в день" создает дефицит и приучает к ценности упражнения. | Новички, которые только пробуют медитацию. |
| **Zen Lite (Подписка)** | **"Низкий порог входа"**. Небольшой ежемесячный платеж за снятие всех лимитов. Психология: "Это стоит как одна чашка кофе, но дает спокойствие на месяц". | Те, кто уже втянул в ритм и не хочет прерывать практику. |
| **Inner Circle (Годовой)** | **"Инвестиция в себя"**. Большая скидка при оплате за год. Психология: "Я серьезно намерен изменить свою жизнь и экономлю 50%". | Лояльные пользователи, ищущие выгоду. |
| **Eternal Peace (Lifetime)** | **"Один раз и навсегда"**. Психология: "Я ненавижу подписки, я хочу купить этот инструмент как молоток и владеть им вечно". | Состоятельные пользователи, ценящие простоту и владение. |

---

## 2. Формат данных (Policy-based Access)

Вместо того чтобы хардкодить `if (plan === 'premium')`, мы будем использовать систему **Политик (Policies)**. Это позволит тебе менять правила игры на сервере, не переписывая фронтенд.

### Модель тарифа на сервере:

```typescript
interface AccessPolicy {
  dailyLimit: number | 'unlimited'; // Сколько раз можно дышать в день
  canUseAI: boolean;                // Доступ к AI-подбору
  customTechniques: boolean;        // Возможность создавать свои паттерны
  premiumThemes: boolean;           // Доступ к эксклюзивным цветам/звукам
}

interface PricingPlan {
  id: string;
  name: string;
  price: number;
  currency: string;
  period: 'month' | 'year' | 'lifetime';
  policy: AccessPolicy;
}

```

---

## 3. Логика проверки на сервере (NestJS)

Чтобы фронтенд знал, можно ли запустить технику, сервер должен отдавать не просто список, а объект с флагом `isLocked`.

### Пример ответа API `/user/techniques`:

```json
[
  {
    "id": "stress_relief",
    "name": "Анти-стресс",
    "isPremium": false,
    "usageLeftToday": 1, 
    "isLocked": false
  },
  {
    "id": "deep_sleep_pro",
    "name": "Глубокий сон PRO",
    "isPremium": true,
    "isLocked": true,
    "unlockReason": "requires_premium"
  }
]

```

### Реализация Guards в NestJS:

```typescript
// Простой пример логики проверки доступа
async canUserBreathe(userId: string, techniqueId: string): Promise<boolean> {
  const user = await this.userService.findById(userId);
  const technique = await this.techService.findById(techniqueId);
  
  // 1. Если техника платная, а у пользователя нет премиума — отказ
  if (technique.isPremium && !user.isPremium) return false;
  
  // 2. Если техника бесплатная, проверяем дневной лимит для обычных юзеров
  if (!user.isPremium) {
    const usagesToday = await this.statsService.getTodayCount(userId);
    if (usagesToday >= user.accessPolicy.dailyLimit) return false;
  }
  
  return true;
}

```

---

## 4. Как это будет работать на фронтенде

Когда пользователь нажимает на круг в `BreathingCircle.svelte`, мы делаем следующее:

1. **Локальная проверка**: Если в сторе `user.isPremium === false` и техника помечена как `isPremium`, сразу открываем `PurchasePanel`.
2. **Проверка лимита**: Если техника бесплатная, но `usageLeftToday === 0`, показываем сообщение: *"Вы сегодня уже практиковались. Хотите еще? Перейдите на Premium"*.

### Рекомендация по реализации:

Я предлагаю использовать готовое решение для управления подписками в Telegram — **RevenueCat** (через их API) или просто хранить таблицу `subscriptions` в своей базе данных, так как Telegram Stars передает вам `payment_payload`, по которому легко идентифицировать покупку.

**Хотите, чтобы я набросал структуру базы данных (SQL), которая свяжет юзеров, покупки и эти политики доступа?** 📊 Она поможет вам легко добавлять новые тарифы в будущем.

Использование единого статуса вместо набора флагов — это более чистый подход (State Machine). Это исключает логические ошибки, когда у вас одновременно может стоять `isPremium: false` и `isLocked: false`.

Предлагаю ввести перечисление (Enum) статусов доступа, которые покроют все психологические и технические сценарии.

---

### 1. Система статусов доступа (Policy Status)

Вместо `boolean` флагов, каждая техника или сессия теперь имеет один из 4-х статусов:

| Статус | Код | Психологический триггер | Поведение на фронтенде |
| --- | --- | --- | --- |
| **Available** | `available` | "Действуй сейчас" | Кнопка активна, запуск разрешен. |
| **Limit Reached** | `limit_reached` | "Дефицит" | Иконка часов. "Вы сегодня уже дышали, возвращайтесь завтра или купите Pro". |
| **Premium Only** | `premium_only` | "Любопытство и статус" | Иконка замка или короны. "Эта техника доступна только в Zen-тарифе". |
| **Locked** | `locked` | "Прогрессия" | Иконка замочка. "Пройдите 3 базовые сессии, чтобы открыть эту". |

---

### 2. Формат данных для сервера (NestJS / JSON API)

Теперь объект техники выглядит так. Сервер сам вычисляет статус на основе данных пользователя и истории его сессий за сегодня.

```json
{
  "id": "square_breathing",
  "name": "Квадратное дыхание",
  "accessStatus": "limit_reached", 
  "policy": {
    "type": "daily_free",
    "limit": 1,
    "remaining": 0
  }
}

```

---

### 3. Реализация в BreathingCircle (Svelte)

Теперь логика клика становится прозрачной и легко масштабируемой.

```svelte
<script>
    import { breathingController } from '../lib/stores/breathing.js';
    
    export let technique; // Объект техники с сервера

    function handleAction() {
        switch (technique.accessStatus) {
            case 'available':
                breathingController.start(technique.patterns);
                break;
                
            case 'limit_reached':
                // Показываем модалку "Лимит исчерпан"
                openPurchase('daily_limit');
                break;
                
            case 'premium_only':
                // Показываем экран продажи тарифа
                openPurchase('premium_features');
                break;
                
            case 'locked':
                showToast("Нужно больше практики, чтобы открыть это");
                break;
        }
    }
</script>

```

---

### 4. Почему это лучше для психологии?

1. **Прозрачность**: Пользователь сразу понимает *почему* он не может нажать кнопку (видит разный визуал для лимита и для премиума).
2. **Гибкость тарифов**: Вы можете легко добавить новый статус (например, `trial`) на сервере, и фронтенд просто добавит один `case` в логику, не ломая старые флаги.
3. **Управление нагрузкой**: Если сервер перегружен, вы можете временно всем бесплатным пользователям отдать `limit_reached`, просто поменяв одну строчку в базе.

### 5. Готовое решение для политики доступа (Node.js)

Если вы хотите внедрить это на серверной части (NestJS), рекомендую использовать паттерн **Strategy**.

```typescript
// Пример логики на сервере
getAccessStatus(user, technique, sessionsToday): AccessStatus {
  if (user.isPremium) return 'available';
  if (technique.isPremiumOnly) return 'premium_only';
  if (sessionsToday >= 1) return 'limit_reached';
  return 'available';
}

```

**Хотите, чтобы я подготовил CSS-стили для оформления этих состояний (замочки, серые фильтры, иконки лимита), чтобы они органично смотрелись в BreathingCircle?** 🎨

Хорошо, фокусируемся исключительно на **серверной архитектуре (NestJS/Node.js)** и структуре данных. Мы переходим от логики «да/нет» к сервис-ориентированной модели доступа.

### 1. Перечисление статусов (Server-side Enum)

На стороне сервера мы определяем четкий контракт. Это то, что будет приходить во фронтенд в поле `accessStatus`.

```typescript
export enum AccessStatus {
  AVAILABLE = 'available',       // Можно играть
  LIMIT_REACHED = 'limit_reached', // Бесплатный лимит на сегодня исчерпан
  PREMIUM_ONLY = 'premium_only',   // Техника только для платных тарифов
  LOCKED = 'locked'                // Заблокировано прогрессом (например, не пройдено обучение)
}

```

### 2. Справочник иконок (Meta-data)

Чтобы фронтенд не гадал, какую иконку рисовать, сервер может отдавать объект метаданных вместе со статусом. Это позволяет вам менять иконки (например, на праздничные) без обновления кода приложения.

| Статус | Иконка (Emoji/SVG ID) | Смысл |
| --- | --- | --- |
| `available` | `play` или `null` | Доступ открыт |
| `limit_reached` | `timer` ⏳ | Психология: "Приходи завтра" |
| `premium_only` | `crown` 👑 | Психология: "Ценный контент" |
| `locked` | `lock` 🔒 | Психология: "Нужно заслужить" |

---

### 3. Модель политики доступа (Policy Object)

Вместо плоских полей в БД, мы создаем структуру **Policy**, которую сервер рассчитывает «на лету» для конкретного пользователя.

```typescript
// Интерфейс ответа сервера для техники
interface TechniqueResponse {
  id: string;
  slug: string;
  accessStatus: AccessStatus;
  icon: string; // Иконка статуса (напр. 'crown')
  
  // Дополнительная информация для UI
  policyInfo?: {
    remainingToday?: number;
    resetTime?: string; // Когда обновится лимит
  }
}

```

### 4. Логика сервиса (Business Logic)

Пример того, как в NestJS сервисе должен происходить расчет статуса. Это «сердце» системы тарифов:

```typescript
@Injectable()
export class AccessService {
  async calculateAccess(user: User, technique: Technique): Promise<TechniqueResponse> {
    // 1. Приоритет: Премиум-пользователь
    if (user.isPremium) {
      return this.formatResponse(technique, AccessStatus.AVAILABLE);
    }

    // 2. Если техника только для PRO
    if (technique.isPremiumOnly) {
      return this.formatResponse(technique, AccessStatus.PREMIUM_ONLY, 'crown');
    }

    // 3. Проверка дневного лимита (бесплатный уровень)
    const sessionsToday = await this.sessionRepo.countToday(user.id);
    const dailyLimit = 1; // Можно вынести в конфиг тарифа

    if (sessionsToday >= dailyLimit) {
      return this.formatResponse(technique, AccessStatus.LIMIT_REACHED, 'timer');
    }

    // 4. По умолчанию
    return this.formatResponse(technique, AccessStatus.AVAILABLE);
  }

  private formatResponse(tech, status, icon = null) {
    return { ...tech, accessStatus: status, icon };
  }
}

```

### Почему такая серверная часть идеальна:

1. **Масштабируемость**: Если завтра вы решите, что в "Energy" можно дышать 3 раза, а в "Sleep" — 1, вы просто меняете `dailyLimit` в базе или конфиге сервера.
2. **Безопасность**: Фронтенд не может «подделать» доступ, так как сервер просто не отдаст массив паттернов `[{inhale...}]` для техники, если статус не `available`.
3. **Гибкость**: Вы можете добавить статус `trial_active` или `sale_active`, и фронтенд просто отобразит новую иконку.

**Будет ли полезно составить структуру таблицы `user_subscriptions` для хранения данных о купленных тарифах?** 💳 Это поможет связать NestJS с платежами через Telegram Stars.